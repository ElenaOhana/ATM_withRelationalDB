package Card;

public class CardService implements ICardService{

    @Override
    public ICard createCard(String name, String pin, int id, String accountNumber) {
        //ICard card = new Card(name, pin, id);
        return null;
    }
}
